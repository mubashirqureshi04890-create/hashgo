
export interface AppInfo {
  version: string;
  size: string;
  updatedAt: string;
  compatibility: string;
}

export interface MiningFeature {
  title: string;
  description: string;
  icon: string;
}
